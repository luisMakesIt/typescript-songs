# Typescript Songs

Usando clases, interfaces y tipos con TS

![Typescript Songs overview](docs/assets/typescript-songs-overview.svg)

## Stack

- Node.js
- JavaScript/TypeScript

## What this repository contains

This repository contains the source code and documentation for **typescript-songs**. The README was refreshed to make the project easier to understand, run and validate from GitHub.

## Project image

The image above represents the current project state. When a local browser runtime was available, it was captured from the running project; otherwise it is an honest architecture/overview image based on source inspection.

## Getting started

```bash
git clone https://github.com/luisMakesIt/typescript-songs.git
cd typescript-songs
```

### Install dependencies

```bash
npm install
```

### Run locally

```bash
npm run dev
```

## Available scripts / commands

| Command | Description |
| --- | --- |
| `test` | `echo "Error: no test specified" && exit 1` |
| `dev` | `tsc -t es6 index.ts && node index.js` |

## Validation notes

- Source inspection completed.

## Suggested next improvements

- Add automated tests or CI if the project does not have them yet.
- Keep environment-specific values out of version control.
- Document any external services required to run the project locally.
